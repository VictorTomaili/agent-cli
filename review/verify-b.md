# Verify-b — Fact-check of Tier 3 / theme claims in AGENT-EXPERIENCE-REVIEW.md

**Method**: read every cited source anchor, then live-probe the CLI against an
isolated `AGENT_CLI_HOME` (`review/_verify-home`, since `util.js:12` honors
`AGENT_CLI_HOME`). Package: `@tomaili/agent` v0.2.1. `src/cli.js` is 2127 lines
(Get-Content), so all cited line ranges (up to 2126) are in bounds. No source
files were modified.

| # | Claim (short) | Anchor | Verdict | Evidence |
|---|---|---|---|---|
| 1 | `fail()` builds `{ok:false, error, ...details}`; ANSI (`c.cyan`) can land inside JSON errors | `src/cli.js:93-98`, `src/commands/target.js:32` | CONFIRMED | `fail()` stringifies `{ok:false, error: message, ...details}` verbatim (cli.js:93-98). `target.js:32` passes `c.cyan("agent targets")` into the message. Probe: `target enable bogus --json` → `{"ok":false,"error":"Unknown target: bogus. Run \u001b[36magent targets\u001b[39m.",...}`, exit 1. Nuance: the cyan is at the call site; `fail()` does not strip it. |
| 2 | `identity apply <unknown>` / `soul apply <unknown>` diverge: human-mode `fail()` vs JSON-mode fallback (`general-purpose`/`pragmatist`) | `src/cli.js:770-794`, `831-851` | CONFIRMED | `identity`: `!known && !JSON_MODE → fail()` (772-776); JSON mode applies anyway (777) and emits `fallback:true, resolved:"general-purpose"` (788). Probe: `identity apply nosuchid --json` → exit 0, wrote IDENTITY.md (general-purpose template); human mode → exit 1. `soul`: same shape (836-849), fallback `"pragmatist"`; probe exit 0. |
| 3 | `consolidate` on empty/no-lessons-dir install fails `{ok:false, reason}` | `src/cli.js:1207-1219` | CONFIRMED | `consolidate()` returns `{ok:false, reason:"no lessons dir", dir}` (`src/consolidate.js:253`); cli.js emits it then `process.exit(1)` (1215-1218). Probe: fresh home `consolidate --json` → `{ok:false, reason:"no lessons dir"}`, exit 1. |
| 4 | `agent help` / bare `agent` hit the commander catch, which whitelists `commander.helpDisplayed`/`commander.version` but not `commander.help` | `src/cli.js:2108-2126`, `2111-2114` | CONFIRMED | Catch at 2111-2114 whitelists only `helpDisplayed` and `version`; `commander.help` falls through to `{ok:false}` + `exit(e.exitCode || 1)`. Live probes: bare `agent` → exit 1, help text on **stderr**; `agent help` → exit 1, stderr `\u001b[31m✗\u001b[39m (outputHelp)`; `agent help link` → exit 1, same; `--help`/`-h` → exit 0; `--version` → exit 0. |
| 5 | `models resolve <missing>` emits `resolved:null`, exits 0 (no fail) | `src/cli.js:989-999` | CONFIRMED | `resolve` never calls `fail()`; emits `{command:"models", action, alias, resolved: r}` where `r = alias ? m.getAlias(alias) : null` (991-992). Probe: `models resolve nosuchalias --json` → `{"resolved": null}`, exit 0. |
| 6 | `where -p` hardcodes `master: MASTER_FILE` | `src/cli.js:1270` | CONFIRMED | Line 1270: `emit({ command:"where", scope, master: MASTER_FILE, targets: rows })` — always global `MASTER_FILE` even when `scope === "project"` (human line 1272 too). Probe: `where -p --json` → `"scope":"project"` but `"master":"<home>\.agents\AGENTS.md"` (global home path). |
| 7 | `skillVersion()` spawns a subprocess per call | `src/skill.js:104-116` | CONFIRMED | `skillVersion()` calls `runSkill(["--version"])` (106), which does `spawnSync(process.execPath, [SUBMODULE_CLI, ...])` (78-85) — a fresh Node child per call. Consumed by `status` (cli.js:440), `brief` (1789), doctor, etc. |
| 8 | Skill subcommands ignore `--json` (no JSON handling) | `src/skills/cli.js`, `src/skills/commands/*` | CONFIRMED | Grep for `--json` across `src/skills` → 0 matches. `skills/cli.js` dispatches purely on `cmd` token; commands like `enable.js` print colored text with no JSON branch. Probe: `agent skill list --json` wraps ANSI-laden text as an escaped string in `{output: "\u001b[1mskill list\u001b[22m..."}` via the passthrough at cli.js:1509-1522 — not structured JSON. |
| 9 | Scope defaults inverted: root cmds default global (`-p`=project) vs skill cmds default project (`-g`=global) | `src/cli.js` link 351-360, edit 583-588, lessons 1044/1060; `src/skills/commands/enable.js:6` | CONFIRMED | Root: link pushes `"global"` when neither flag (360); edit `-p, --project` else global (584-588); lessons `opts.project ? "project" : "global"` (1060). Skill: `enable.js:6` `global = args.includes('-g')`, default project (24-29). |
| 10 | `brief`/`doctor`/`update list` call `saveConfig(cfg)` after `ensureUpdateCheck` | `src/cli.js:1312, 1734, 1795` | CONFIRMED | `update list`: `ensureUpdateCheck` 1309-1311 → `if (upd.refreshed) await saveConfig(cfg)` 1312. `doctor`: 1731-1733 → 1734. `brief`: 1794 → 1795. All three mutate config as a read-command side effect. |
| 11 | `link --target bogus` silently yields `results: []` | `src/cli.js:156-162` (used at 366) | CONFIRMED | `selectedTargets` filters the pool by id set (160), dropping unknown ids; link loops over the empty list, emits `{command:"link", scopes:["global"], results: []}`, no `fail()` (355-379). Probe: `link --target bogus --json` → `{"results": []}`, exit 0. |
| 12 | `edit` kinds are agents\|soul\|identity\|user\|lessons\|environments, no models | `src/cli.js:578-596` | CONFIRMED | Description lists exactly those kinds (580); unknown kind → `fail("Unknown kind: ... agents|soul|identity|user|lessons|environments")` (592-596). No `models` branch anywhere in the handler. |

## Notes

- All 12 claims verified: **12 CONFIRMED, 0 WRONG/UNVERIFIED/PARTIAL**.
- Claim 1 nuance: `fail()` itself is ANSI-free; the cyan enters via `target.js:32` and other call sites, and `fail()` serializes the message unstripped — so the review's "ANSI in JSON payloads" is accurate, but the fix belongs at the call sites / a strip in `fail()`/`emit`.
- Claim 4 verified both by code and empirically; the review's `✗ (outputHelp)` stderr leak is real and reproducible.
- Scratch artifacts from probing (`review/probe.cmd`, `probe2.cmd`, `review/_out*.json`, `_err*.txt`, `review/_verify-home/`) could not be deleted in this environment (EPERM / destructive gate); they contain only isolated-home probe output and can be removed manually.
